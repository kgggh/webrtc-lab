general: {
    loggers: ["stdout"]   # 로그 출력 방식 (파일 로그는 ["file"] 사용)
    log_level: "info"     # 로그 레벨 (debug, info, warn, error 가능)
    daemonize: false      # 데몬 모드 여부 (서비스 실행 방식에 따라 결정)
}

nat: {
    stun_server = "stun.l.google.com"
    port = 19302
}
